{
  "name": "scalable-products-api",
  "version": "1.0.0",
  "description": "REST API demonstrating read/write splitting across PostgreSQL primary and replica nodes.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
